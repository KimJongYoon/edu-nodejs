doctype html
html
    head
        meta(charset='utf-8')
    body
        h1
            a(href='/topic') Server Side JavaScript
        ul
            each topic in topics
                li 
                    - rid = encodeURIComponent(topic['@rid']);
                    a(href=`/topic/${rid}`)= topic['title']
        article
            if topic[0]
                h2= topic[0].title
                = topic[0].desc
            else
                h2 Welcom
                | This is Server side Javascript Tutorial
        p
        div
            a(href=`/topic/add`) add
            | 
            if topic[0]
                - thisRid = encodeURIComponent(topic[0]['@rid']);
                a(href=`/topic/${thisRid}/edit`) edit
                | 
                a(href=`/topic/${thisRid}/del`) del